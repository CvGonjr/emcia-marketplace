---
description: Mostra, para o nível apurado do caso, quais etapas são preparáveis e quais exigem pessoa.
---
Execute `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/fronteira.py"` e apresente o resultado.

Não interprete além do que a tabela diz, e **não trate a fronteira como negociável**: a camada de cada etapa é declarada no playbook e desloca com o nível do caso (CAT-01 3.6). Se o operador pedir para preparar algo que a tabela marca como humano, diga que a etapa exige pessoa e pare.
